# claude in wechat

> 一个轻量化可接入微信的claude code项目远程管理软件

### 开发目标

1. 具有可视化，可交互的前端页面
2. 具有初始配置功能，全自动脚本绑定微信与PC端claude code
3. 在微信上能监测PC端claude code各项目完成进度，并且能够远程下达任务，选择性接收产出文件
4. 类似于市面上千问，豆包，kimi等手机应用，只不过接入的网站版对话变成了我自己桌面上的claude code本地服务
5. 打包为可维护的项目文件，包含exe安装包，源码等，符合github格式
6. 自动上传到我的github页面，替换掉之前的claude-to-weixin，作为更新替换，保留之前的版本，之前版本是v0.1，现在是v0.2

### 详细功能（微信端）

1. 输入"/open (*project_name*)"打开某个项目
2. 输入"/new (*project_name*)"新建某个项目
3. 输入"/delete (*project_name*)"删除某个项目
4. 输入"/rename(*project_name*)"重命名某项目
5. 输入"/list"列出全部项目
6. 输入"/check (*project_name*)"查询某个项目的进度
7. 输入"/token (*project_name*)"查询某个项目使用的token
8. 决断某个项目的询问

### 详细功能（桌面端）

1. 作为一个中转站，接收微信指令后发布给各claude code终端执行，监控claude code，把claude code的输出返回至微信
2. 当一个项目完成后给微信提示，输出消息"(*project_name*)已完成"
3. 微信基本指令的执行：打开某个项目类似于打开某个文件夹；新建某个项目类似于新建一个文件夹；删除某个项目类似于删除某个文件夹；重命名某个项目类似于重命名某个文件夹；列出全部项目需要包含所有项目名字以及是否在执行任务；查询进度需要给出任务清单完成情况（在claude code里，很多复杂项目都会列清单，如果有就输出，没有就不输出）以及大致的进度条（进度条需要自己估计，这个是额外功能，想办法实现），所有项目保存到~\projects\Wechat，没有这个文件夹，你自己新建
4. 一个项目相当于一个独立的claude code，这时候微信在这个项目对话相当于间接和claude code对话，此软件只做到转接的作用
5. 微信指令收到要回复"收到"，微信在项目里给claude code对话，每一句都要先回复"收到"再回复claude code说的内容，如果项目较大，不能把claude code的回复全部输出给微信，只需要做决断和最后完成的输出
6. 有些项目claude code会让我做选择，给出几个选项让我选，这时候要把选项返回到微信里，我可以在微信做选择，并且能成功返回到claude code让它正常执行
7. 软件本体可交互界面只需要显示每个项目的名称，是否完成，进度条，token，以及按键，我点击之后自动弹出那个项目的claude code界面，claude code平时在后台运行，不显示出来

### 可能出现的bug

1. 选项功能有时候可能微信上做了选择，但是桌面端还需要做，目标应该是微信上回复了桌面端就不用回复
2. 微信上指令有时候没有返回，首先要把"收到"做好，以此判断是否接收消息，而且指令之间不能互相干扰，缓存要做好

### 注意

1. 软件运行环境是windows 11系统，具体的兼容性考虑你自己想
2. 既有前端的可视化界面，又有后端的桥接脚本
3. 开机自启动，尽量留住系统进程
4. 需要对claude code本体进行配置，比如hook，memory等
5. 参考~\\.claude\skills\claude-to-im, ~\\.claude\skills\claude-to-im-backend, ~\.claude\skills\weixin-decison, ~\\.claude\skills\weixin-global-integration
6. 参考github上类似项目
7. 先计划，再执行
8. 如果有更好的想法，与我交流，让我选择，但是我的要求必须实现

